// Meer info over tempo enzo: https://strudel.cc/workshop/first-sounds/
// Eén cycle is standaard 2 seconden.
// Samenvatting: https://strudel.cc/workshop/first-sounds/#recap

// MQTT synchroon met elke bass drum (bd)
const soundPattern = "bd bd hh bd rim bd hh bd, - - - - jazz - - -"
const mqttPattern  = "on on -  on -   on -  on, - - - - -    - - -"  // Matches bd positions

stack(
  sound(soundPattern).scope(),
  sound(mqttPattern).mqtt('strudel', 'qifj3258', 'strudel/control', 'wss://mqtt.rubu.be:9003/mqtt', 'strudelid437354', 0)
)

// Optie 2: Origineel (met handmatig MQTT patroon)
// const soundConst = sound("bd bd hh bd rim bd hh bd, - - - - jazz - - -")
// const mqttConst = sound("- - - - on - - -")
//
// stack(
//   soundConst.scope(),
//   mqttConst.mqtt('strudel', 'qifj3258', 'strudel/control', 'wss://mqtt.rubu.be:9003/mqtt', 'strudelid437354', 0)
// )
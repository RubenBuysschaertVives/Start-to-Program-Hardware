// Meer info over tempo enzo: https://strudel.cc/workshop/first-sounds/
// Eén cycle is standaard 2 seconden.
// Samenvatting: https://strudel.cc/workshop/first-sounds/#recap

const soundConst = sound("bd bd hh bd rim bd hh bd, - - - - jazz - - -")
const mqttConst = sound("- - - - on - - -")

stack(
  soundConst.scope(),
  mqttConst.mqtt('strudel', 'qifj3258', 'strudel/control', 'wss://mqtt.rubu.be:9003/mqtt', 'strudelid437354', 0)
  )
  
  
  
Stap 1
------
// Een constante maken met daarin een functie met geluiden uit de drums.
const soundConst = sound("bd hh")

// De constante 'oproepen'...
soundConst


Stap 2
------
// Een constante maken met daarin een functie met geluiden uit de drums.
const soundConst = sound("bd bd hh bd rim bd hh bd")

// De constante 'oproepen'...
soundConst


Stap 3
------
// Een constante maken met daarin een functie met geluiden uit de drums.
const soundConst = sound("bd bd hh bd rim bd hh bd")

// De constante 'oproepen' en het geluid visualiseren...
soundConst.scope()


Stap 4
------
// Een constante maken met daarin een functie met geluiden uit de drums.
const soundConst = sound("bd bd hh bd rim bd hh bd")

// Een constante maken met daarin aangegeven wanneer de LED's moeten knipperen.
const mqttConst = sound("- - - - on - - -")

// Een stack (stapel) aanmaken met 'dingen om te doen'...
stack(
	// De constante 'oproepen' en het geluid visualiseren...
	soundConst.scope(),
	
	// Verbinding maken met een MQTT-broker en berichten verzenden.
	mqttConst.mqtt('strudel', 'qifj3258', 'strudel/control', 'wss://mqtt.rubu.be:9003/mqtt', 'strudelid437354', 0)
)


Stap 5 (jazz toevoegen)
-----------------------
// Een constante maken met daarin een functie met geluiden uit de drums.
const soundConst = sound("bd bd hh bd rim bd hh bd, - - - - jazz - - -")

// Een constante maken met daarin aangegeven wanneer de LED's moeten knipperen.
const mqttConst = sound("- - - - on - - -")

// Een stack (stapel) aanmaken met 'dingen om te doen'...
stack(
	// De constante 'oproepen' en het geluid visualiseren...
	soundConst.scope(),
	
	// Verbinding maken met een MQTT-broker en berichten verzenden.
	mqttConst.mqtt('strudel', 'qifj3258', 'strudel/control', 'wss://mqtt.rubu.be:9003/mqtt', 'strudelid437354', 0)
)
